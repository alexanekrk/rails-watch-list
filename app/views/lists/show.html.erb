<h1><%= @list.name %></h1>

<ul>
<% @list.each do |l| %>
  <%= l.bookmark %>
<% end %>
  <li></li>
</ul>

<%= link_to "Add a movie", new_list_bookmark_path(@list) %>

<%= link_to "Back to all lists", lists_path(@list) %>
